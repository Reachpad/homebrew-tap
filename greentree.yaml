version: 1
checks:
  formula:
    run: ruby --disable-gems -c Formula/reachpad.rb && ruby --disable-gems -c scripts/update-reachpad.rb && ruby --disable-gems scripts/update-reachpad.rb --check
    required_for_publish: true
    watch: true
